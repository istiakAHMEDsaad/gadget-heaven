/* import { createContext, useState } from "react";

const MyContextApi = createContext();

export const ContexProp = (props) => {
    const [money, setMoney] = useState(0);
  return (
    <MyContextApi.Provider value={money}>
        {props.children}
    </MyContextApi.Provider>
  )
}
 */